// Vercel/public deployment settings. Safe to expose in browser source.
// Set apiBaseUrl to the public HTTPS URL of the persistent Node backend.
// wsUrl is optional; when omitted it is derived from apiBaseUrl.
window.HAMU_PUBLIC_CONFIG = {
  apiBaseUrl: '',
  wsUrl: ''
};
